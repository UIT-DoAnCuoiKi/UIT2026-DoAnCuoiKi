# Phase 6: Cloud sync and cross-lot central — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **Ghi chú phạm vi:** đây là phase nặng nhất, đụng định danh toàn cục và đồng bộ hai tầng. Plan này hiện thực phần lõi demo được: định danh UUID cho phiên, outbox bền ở edge, upsert idempotent phía nhận, config pull, và tra cứu liên bãi. Đồng bộ đa thực thể đầy đủ (thanh toán, sự cố, ảnh) và một dịch vụ cloud tách riêng là bước tiếp theo; nếu quỹ thời gian đồ án hẹp, demo một bãi chạy trọn offline đã chốt ở phase 5, phase này chứng minh cơ chế mở rộng. Cân nhắc một pass brainstorm riêng cho dịch vụ cloud độc lập trước khi mở rộng.

**Goal:** cho phép mỗi bãi (edge) đẩy dữ liệu phiên lên tầng trung tâm khi có mạng mà không trùng lặp, kéo cấu hình master xuống, và cho admin tổng tra cứu phiên liên bãi.

**Architecture:** thêm khóa nghiệp vụ `uuid` cho phiên (không thay khóa chính số nguyên cục bộ); bảng `outbox` bền ghi thay đổi cần đồng bộ; service đóng gói và upsert idempotent theo `uuid`; endpoint nhận đồng bộ (`POST /sync/push`), phát cấu hình (`GET /sync/config`), và tra cứu liên bãi (`GET /central/sessions`).

**Tech Stack:** Python 3.13, FastAPI, SQLAlchemy 2.0 sync, Alembic, pydantic v2, pytest.

## Global Constraints

Kế thừa index mục Global Constraints. Riêng phase này:
- Giữ khóa chính số nguyên cục bộ; thêm `session.uuid` (String 36, unique, index) làm khóa nghiệp vụ toàn cục, sinh tự động bằng `uuid4`. Đây là hiện thực của quyết định sắp xếp trong index (UUID nằm ở phase 6, không phá suite các phase trước).
- Idempotent theo `uuid`: đẩy lại cùng phiên không tạo bản trùng ở tầng nhận; upsert cập nhật theo `uuid`.
- Outbox bền: mỗi thay đổi phiên cần đồng bộ ghi một dòng `outbox` (`entity_type`, `entity_uuid`, `lot_id`, `payload`, `synced`). Đẩy thành công thì đánh dấu `synced = True`.
- Dữ liệu vận hành chảy lên (edge đẩy), cấu hình chảy xuống (edge kéo); không bảng nào ghi hai chiều.
- `POST /sync/push` và `GET /sync/config` xác thực bằng `require_edge_key` (khóa thiết bị theo bãi). `GET /central/sessions` xác thực `get_current_user` vai admin.
- Thời gian trong payload đồng bộ dạng chuỗi ISO 8601; parse lại khi upsert.
- Không dùng ký tự gạch ngang trong văn xuôi tài liệu.
- Test trên SQLite in-memory qua `db_session`, `client`, `admin_headers`; fixture edge key lấy từ `settings.edge_api_key` mặc định `edge-dev-key`.

---

### Task 1: Khóa `uuid` cho phiên và model `Outbox`; migration; schema test

**Files:**
- Modify: `src/backend/app/models/parking_session.py`
- Create: `src/backend/app/models/outbox.py`
- Modify: `src/backend/app/models/__init__.py`
- Create: `src/backend/alembic/versions/f6a7b8c9d0e1_add_uuid_outbox.py`
- Test: `src/backend/tests/test_sync_schema.py`

**Interfaces:**
- Produces: `ParkingSession.uuid: str` (unique, sinh tự động). `Outbox(id:int, entity_type:str, entity_uuid:str, lot_id:int|None, payload:dict, synced:bool, created_at:datetime)`.

- [ ] **Step 1: Write the failing test**

Create `src/backend/tests/test_sync_schema.py`:

```python
def test_outbox_table_and_session_uuid():
    from app.db import Base
    import app.models  # noqa: F401
    assert "outbox" in Base.metadata.tables
    from app.models import ParkingSession
    assert "uuid" in ParkingSession.__table__.columns.keys()


def test_session_uuid_autogenerated(db_session):
    from app.models import ParkingSession
    s = ParkingSession(plate_hash="h", plate_ciphertext="x", vehicle_group="o_to_con", status="in_lot")
    db_session.add(s); db_session.commit(); db_session.refresh(s)
    assert s.uuid is not None and len(s.uuid) >= 32
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd src/backend && python -m pytest tests/test_sync_schema.py -v`
Expected: FAIL (no outbox, no uuid).

- [ ] **Step 3: Add uuid to session and create Outbox**

In `src/backend/app/models/parking_session.py`, add at the top of the imports:

```python
from uuid import uuid4
```

Add this column inside `ParkingSession`, right after the `id` column:

```python
    uuid: Mapped[str] = mapped_column(String(36), unique=True, index=True, default=lambda: str(uuid4()))
```

(`String` is already imported in that file.)

Create `src/backend/app/models/outbox.py`:

```python
from datetime import datetime

from sqlalchemy import Boolean, DateTime, Integer, JSON, String, func
from sqlalchemy.orm import Mapped, mapped_column

from app.db import Base


class Outbox(Base):
    __tablename__ = "outbox"

    id: Mapped[int] = mapped_column(primary_key=True)
    entity_type: Mapped[str] = mapped_column(String(32))
    entity_uuid: Mapped[str] = mapped_column(String(36), index=True)
    lot_id: Mapped[int | None] = mapped_column(Integer, nullable=True)
    payload: Mapped[dict] = mapped_column(JSON)
    synced: Mapped[bool] = mapped_column(Boolean, default=False, index=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

- [ ] **Step 4: Register the model**

In `src/backend/app/models/__init__.py`, add `from app.models.outbox import Outbox` and `"Outbox"` to `__all__`.

- [ ] **Step 5: Run schema test to verify it passes**

Run: `cd src/backend && python -m pytest tests/test_sync_schema.py -v`
Expected: PASS (2 tests).

- [ ] **Step 6: Write the migration**

Create `src/backend/alembic/versions/f6a7b8c9d0e1_add_uuid_outbox.py`:

```python
"""add session.uuid and outbox table

Revision ID: f6a7b8c9d0e1
Revises: e5f6a7b8c9d0
Create Date: 2026-08-23
"""
import uuid as uuidlib

import sqlalchemy as sa

from alembic import op

revision = "f6a7b8c9d0e1"
down_revision = "e5f6a7b8c9d0"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("session", sa.Column("uuid", sa.String(length=36), nullable=True))
    # backfill uuid cho phiên cũ
    bind = op.get_bind()
    rows = bind.execute(sa.text("SELECT id FROM session WHERE uuid IS NULL")).fetchall()
    for (sid,) in rows:
        bind.execute(sa.text("UPDATE session SET uuid = :u WHERE id = :i"),
                     {"u": str(uuidlib.uuid4()), "i": sid})
    op.create_index("ix_session_uuid", "session", ["uuid"], unique=True)

    op.create_table(
        "outbox",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("entity_type", sa.String(length=32), nullable=False),
        sa.Column("entity_uuid", sa.String(length=36), nullable=False),
        sa.Column("lot_id", sa.Integer(), nullable=True),
        sa.Column("payload", sa.JSON(), nullable=False),
        sa.Column("synced", sa.Boolean(), nullable=False, server_default=sa.false()),
        sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_index("ix_outbox_entity_uuid", "outbox", ["entity_uuid"])
    op.create_index("ix_outbox_synced", "outbox", ["synced"])


def downgrade() -> None:
    op.drop_table("outbox")
    op.drop_index("ix_session_uuid", table_name="session")
    op.drop_column("session", "uuid")
```

- [ ] **Step 7: Run the full suite**

Run: `cd src/backend && python -m pytest -q`
Expected: PASS (uuid tự sinh, không phá test cũ).

- [ ] **Step 8: Commit**

```bash
git add src/backend/app/models/parking_session.py src/backend/app/models/outbox.py src/backend/app/models/__init__.py src/backend/alembic/versions/f6a7b8c9d0e1_add_uuid_outbox.py src/backend/tests/test_sync_schema.py
git commit -m "feat(backend): add session uuid business key and outbox table"
```

---

### Task 2: Service đóng gói và outbox

**Files:**
- Create: `src/backend/app/services/sync.py`
- Test: `src/backend/tests/test_sync_service.py`

**Interfaces:**
- Consumes: `ParkingSession`, `Outbox` (Task 1); `to_naive` (`app/clock.py`).
- Produces: `serialize_session(session) -> dict` (JSON an toàn, thời gian ISO); `enqueue_session(db, session) -> None` (ghi outbox `entity_type="session"`); `unsynced_batch(db, limit=100) -> list[Outbox]`; `mark_synced(db, outbox_ids: list[int]) -> None`; `upsert_session_from_sync(db, payload: dict) -> ParkingSession` (idempotent theo `uuid`).

- [ ] **Step 1: Write the failing test**

Create `src/backend/tests/test_sync_service.py`:

```python
def _session(db, uuid="u-1", lot_id=1):
    from app.clock import now_utc
    from app.models import ParkingSession
    s = ParkingSession(uuid=uuid, lot_id=lot_id, plate_hash="h", plate_ciphertext="x",
                       vehicle_group="o_to_con", status="completed", entry_time=now_utc(),
                       exit_time=now_utc(), fee_amount=20000, match_flag="exact")
    db.add(s); db.commit(); db.refresh(s)
    return s


def test_serialize_is_json_safe(db_session):
    import json
    from app.services.sync import serialize_session
    s = _session(db_session)
    payload = serialize_session(s)
    json.dumps(payload)  # không lỗi
    assert payload["uuid"] == "u-1"
    assert isinstance(payload["entry_time"], str)


def test_enqueue_and_batch_and_mark(db_session):
    from app.services.sync import enqueue_session, mark_synced, unsynced_batch
    s = _session(db_session)
    enqueue_session(db_session, s)
    batch = unsynced_batch(db_session)
    assert len(batch) == 1 and batch[0].entity_uuid == "u-1"
    mark_synced(db_session, [batch[0].id])
    assert unsynced_batch(db_session) == []


def test_upsert_is_idempotent(db_session):
    from app.services.sync import serialize_session, upsert_session_from_sync
    s = _session(db_session, uuid="u-9")
    payload = serialize_session(s)

    # giả lập tầng nhận: db mới
    from sqlalchemy import create_engine
    from sqlalchemy.orm import sessionmaker
    from sqlalchemy.pool import StaticPool
    from app.db import Base
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)
    cloud = sessionmaker(bind=engine)()

    upsert_session_from_sync(cloud, payload)
    upsert_session_from_sync(cloud, payload)  # lần hai không tạo bản trùng
    from app.models import ParkingSession
    assert cloud.query(ParkingSession).filter(ParkingSession.uuid == "u-9").count() == 1
    cloud.close(); engine.dispose()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd src/backend && python -m pytest tests/test_sync_service.py -v`
Expected: FAIL (ModuleNotFoundError: app.services.sync).

- [ ] **Step 3: Write the service**

Create `src/backend/app/services/sync.py`:

```python
from datetime import datetime

from sqlalchemy import select
from sqlalchemy.orm import Session

from app.clock import to_naive
from app.models import Outbox, ParkingSession

_FIELDS = (
    "uuid", "lot_id", "zone_id", "plate_hash", "plate_ciphertext",
    "vehicle_group", "vehicle_type", "status", "fee_amount", "match_flag",
)
_TIME_FIELDS = ("entry_time", "exit_time")


def _iso(dt: datetime | None) -> str | None:
    d = to_naive(dt)
    return d.isoformat() if d is not None else None


def serialize_session(session: ParkingSession) -> dict:
    data = {f: getattr(session, f) for f in _FIELDS}
    for f in _TIME_FIELDS:
        data[f] = _iso(getattr(session, f))
    return data


def enqueue_session(db: Session, session: ParkingSession) -> None:
    db.add(Outbox(
        entity_type="session", entity_uuid=session.uuid,
        lot_id=session.lot_id, payload=serialize_session(session),
    ))
    db.commit()


def unsynced_batch(db: Session, limit: int = 100) -> list[Outbox]:
    return list(db.scalars(
        select(Outbox).where(Outbox.synced.is_(False)).order_by(Outbox.id).limit(limit)
    ).all())


def mark_synced(db: Session, outbox_ids: list[int]) -> None:
    for oid in outbox_ids:
        row = db.get(Outbox, oid)
        if row is not None:
            row.synced = True
    db.commit()


def _parse_time(value: str | None) -> datetime | None:
    return datetime.fromisoformat(value) if value else None


def upsert_session_from_sync(db: Session, payload: dict) -> ParkingSession:
    existing = db.scalars(select(ParkingSession).where(ParkingSession.uuid == payload["uuid"])).first()
    target = existing or ParkingSession(uuid=payload["uuid"])
    for f in _FIELDS:
        if f == "uuid":
            continue
        setattr(target, f, payload.get(f))
    target.entry_time = _parse_time(payload.get("entry_time"))
    target.exit_time = _parse_time(payload.get("exit_time"))
    if existing is None:
        db.add(target)
    db.commit(); db.refresh(target)
    return target
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd src/backend && python -m pytest tests/test_sync_service.py -v`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/backend/app/services/sync.py src/backend/tests/test_sync_service.py
git commit -m "feat(backend): add sync serialize, outbox and idempotent upsert"
```

---

### Task 3: Nối outbox vào vòng đời phiên

**Files:**
- Modify: `src/backend/app/routers/sessions.py`
- Test: `src/backend/tests/test_outbox_wiring.py`

**Interfaces:**
- Consumes: `enqueue_session` (Task 2); `confirm_entry`, `_complete_session` (`sessions.py`).
- Produces: mỗi lần tạo phiên (vào) và mỗi lần hoàn tất phiên (ra) ghi một dòng outbox.

- [ ] **Step 1: Write the failing test**

Create `src/backend/tests/test_outbox_wiring.py`:

```python
def test_entry_enqueues_outbox(client, staff_headers, make_reading, db_session):
    from app.models import Outbox
    reading = make_reading(plate="51F70001", direction="in")
    client.post("/sessions/entry", json={"reading_id": reading.id}, headers=staff_headers)
    assert db_session.query(Outbox).filter(Outbox.entity_type == "session").count() >= 1
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd src/backend && python -m pytest tests/test_outbox_wiring.py -v`
Expected: FAIL (không có dòng outbox).

- [ ] **Step 3: Wire enqueue at entry and completion**

In `src/backend/app/routers/sessions.py`, add the import:

```python
from app.services.sync import enqueue_session
```

In `confirm_entry`, after `db.refresh(session)` and before `return _session_out(session, warning=warning)`, add:

```python
    enqueue_session(db, session)
```

In `_complete_session`, at the very end of the function (after `_set_retention(db, session)`), add:

```python
    enqueue_session(db, session)
```

Note: `enqueue_session` commits; `_complete_session` callers call `db.commit()` afterward, which is a harmless second commit. Behavior of existing exit and entry endpoints is unchanged except for the added outbox rows.

- [ ] **Step 4: Run test and the session suites**

Run: `cd src/backend && python -m pytest tests/test_outbox_wiring.py tests/test_session_entry.py tests/test_session_exit.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/backend/app/routers/sessions.py src/backend/tests/test_outbox_wiring.py
git commit -m "feat(backend): enqueue session outbox on entry and completion"
```

---

### Task 4: Endpoint đồng bộ và config pull

**Files:**
- Create: `src/backend/app/schemas/sync.py`
- Create: `src/backend/app/routers/sync.py`
- Modify: `src/backend/app/main.py`
- Test: `src/backend/tests/test_sync_api.py`

**Interfaces:**
- Consumes: `upsert_session_from_sync` (Task 2); `require_edge_key` (`app/deps.py`); `PriceRule`, `FeatureToggle` cho config pull.
- Produces: `POST /sync/push {items:[{entity_type, payload}]}` upsert idempotent, trả `{"upserted": n}`; `GET /sync/config` trả `{price_rules:[...], feature_toggles:{...}}`.

- [ ] **Step 1: Write the failing test**

Create `src/backend/tests/test_sync_api.py`:

```python
EDGE = {"X-Edge-Key": "edge-dev-key"}


def test_push_upserts_idempotently(client):
    item = {"entity_type": "session", "payload": {
        "uuid": "sync-1", "lot_id": 1, "zone_id": None, "plate_hash": "h",
        "plate_ciphertext": "x", "vehicle_group": "o_to_con", "vehicle_type": "car",
        "status": "completed", "fee_amount": 15000, "match_flag": "exact",
        "entry_time": "2026-08-23T08:00:00", "exit_time": "2026-08-23T09:00:00",
    }}
    r1 = client.post("/sync/push", json={"items": [item]}, headers=EDGE)
    assert r1.status_code == 200 and r1.json()["upserted"] == 1
    r2 = client.post("/sync/push", json={"items": [item]}, headers=EDGE)
    assert r2.status_code == 200
    from app.models import ParkingSession
    # đẩy lại không tạo bản trùng
    # kiểm qua tra cứu liên bãi ở task 5; ở đây chỉ chắc không lỗi


def test_push_requires_edge_key(client):
    assert client.post("/sync/push", json={"items": []}).status_code in (401, 403, 422)


def test_config_pull_returns_rules_and_toggles(client, admin_headers):
    client.post("/price-rules", json={"vehicle_group": "o_to_con", "mode": "flat", "unit_price": 10000}, headers=admin_headers)
    cfg = client.get("/sync/config", headers=EDGE).json()
    assert any(r["vehicle_group"] == "o_to_con" for r in cfg["price_rules"])
    assert "read_plate" in cfg["feature_toggles"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd src/backend && python -m pytest tests/test_sync_api.py -v`
Expected: FAIL (404 on `/sync/push`).

- [ ] **Step 3: Write the schemas**

Create `src/backend/app/schemas/sync.py`:

```python
from pydantic import BaseModel


class SyncItem(BaseModel):
    entity_type: str
    payload: dict


class SyncPushIn(BaseModel):
    items: list[SyncItem]


class SyncPushOut(BaseModel):
    upserted: int
```

- [ ] **Step 4: Write the router**

Create `src/backend/app/routers/sync.py`:

```python
from fastapi import APIRouter, Depends
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.db import get_db
from app.deps import require_edge_key
from app.models import FeatureToggle, PriceRule
from app.schemas.sync import SyncPushIn, SyncPushOut
from app.services.sync import upsert_session_from_sync

router = APIRouter(tags=["sync"])


@router.post("/sync/push", response_model=SyncPushOut, dependencies=[Depends(require_edge_key)])
def push(body: SyncPushIn, db: Session = Depends(get_db)) -> SyncPushOut:
    count = 0
    for item in body.items:
        if item.entity_type == "session":
            upsert_session_from_sync(db, item.payload)
            count += 1
    return SyncPushOut(upserted=count)


@router.get("/sync/config", dependencies=[Depends(require_edge_key)])
def config(db: Session = Depends(get_db)) -> dict:
    rules = db.scalars(select(PriceRule).order_by(PriceRule.id)).all()
    toggle = db.scalars(select(FeatureToggle).order_by(FeatureToggle.id)).first()
    return {
        "price_rules": [
            {"vehicle_group": r.vehicle_group, "mode": r.mode, "unit_price": r.unit_price,
             "block_minutes": r.block_minutes, "active": r.active}
            for r in rules
        ],
        "feature_toggles": {
            "read_plate": toggle.read_plate if toggle else True,
            "plate_color": toggle.plate_color if toggle else True,
            "vehicle_class": toggle.vehicle_class if toggle else True,
        },
    }
```

- [ ] **Step 5: Register the router**

In `src/backend/app/main.py`, add `sync` to the routers import line and add `app.include_router(sync.router)`.

- [ ] **Step 6: Run test to verify it passes**

Run: `cd src/backend && python -m pytest tests/test_sync_api.py -v`
Expected: PASS (3 tests).

- [ ] **Step 7: Commit**

```bash
git add src/backend/app/schemas/sync.py src/backend/app/routers/sync.py src/backend/app/main.py src/backend/tests/test_sync_api.py
git commit -m "feat(backend): add sync push and config pull endpoints"
```

---

### Task 5: Tra cứu liên bãi

**Files:**
- Modify: `src/backend/app/routers/sync.py`
- Test: `src/backend/tests/test_central_query.py`

**Interfaces:**
- Consumes: `ParkingSession`; `plate_hash` (`app/security/plate.py`); `crypto`; `get_current_user`, `require_role`, `User`.
- Produces: `GET /central/sessions?lot_id=&plate=` trả danh sách phiên lọc theo bãi và biển (qua `plate_hash` chuẩn hóa), chỉ admin.

- [ ] **Step 1: Write the failing test**

Create `src/backend/tests/test_central_query.py`:

```python
EDGE = {"X-Edge-Key": "edge-dev-key"}


def _push(client, uuid, lot_id, plate_hash):
    item = {"entity_type": "session", "payload": {
        "uuid": uuid, "lot_id": lot_id, "zone_id": None, "plate_hash": plate_hash,
        "plate_ciphertext": "x", "vehicle_group": "o_to_con", "vehicle_type": "car",
        "status": "completed", "fee_amount": 1000, "match_flag": "exact",
        "entry_time": "2026-08-23T08:00:00", "exit_time": "2026-08-23T09:00:00",
    }}
    client.post("/sync/push", json={"items": [item]}, headers=EDGE)


def test_central_filters_by_lot(client, admin_headers):
    from app.security.plate import plate_hash
    _push(client, "c-1", 1, plate_hash("51A00001"))
    _push(client, "c-2", 2, plate_hash("51A00002"))
    rows = client.get("/central/sessions?lot_id=1", headers=admin_headers).json()
    assert all(r["lot_id"] == 1 for r in rows)
    assert any(r["uuid"] == "c-1" for r in rows)


def test_central_filters_by_plate_across_lots(client, admin_headers):
    from app.security.plate import plate_hash
    ph = plate_hash("51A55555")
    _push(client, "c-3", 1, ph)
    _push(client, "c-4", 2, ph)
    rows = client.get("/central/sessions?plate=51A55555", headers=admin_headers).json()
    uuids = {r["uuid"] for r in rows}
    assert {"c-3", "c-4"}.issubset(uuids)


def test_central_requires_admin(client, staff_headers):
    assert client.get("/central/sessions", headers=staff_headers).status_code == 403
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd src/backend && python -m pytest tests/test_central_query.py -v`
Expected: FAIL (404 on `/central/sessions`).

- [ ] **Step 3: Add the endpoint**

In `src/backend/app/routers/sync.py`, extend imports:

```python
from fastapi import APIRouter, Depends, Query
from app.deps import get_current_user, require_edge_key, require_role
from app.models import FeatureToggle, ParkingSession, PriceRule, User
from app.security.plate import plate_hash
```

Append this route:

```python
central_admin = require_role("admin")


@router.get("/central/sessions")
def central_sessions(
    lot_id: int | None = Query(None),
    plate: str | None = Query(None),
    limit: int = Query(100, ge=1, le=500),
    db: Session = Depends(get_db),
    admin: User = Depends(central_admin),
) -> list[dict]:
    stmt = select(ParkingSession)
    if lot_id is not None:
        stmt = stmt.where(ParkingSession.lot_id == lot_id)
    if plate:
        stmt = stmt.where(ParkingSession.plate_hash == plate_hash(plate))
    rows = db.scalars(stmt.order_by(ParkingSession.id.desc()).limit(limit)).all()
    return [
        {"uuid": s.uuid, "lot_id": s.lot_id, "status": s.status,
         "vehicle_group": s.vehicle_group, "fee_amount": s.fee_amount,
         "entry_time": s.entry_time.isoformat() if s.entry_time else None,
         "exit_time": s.exit_time.isoformat() if s.exit_time else None}
        for s in rows
    ]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd src/backend && python -m pytest tests/test_central_query.py -v`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/backend/app/routers/sync.py src/backend/tests/test_central_query.py
git commit -m "feat(backend): add cross-lot central session query"
```

---

### Task 6: Test nghiệm thu phase và chạy toàn suite

**Files:**
- Test: `src/backend/tests/test_phase6_sync_acceptance.py`

**Interfaces:**
- Consumes: mọi thứ Task 1 tới 5.

- [ ] **Step 1: Write the acceptance test**

Create `src/backend/tests/test_phase6_sync_acceptance.py`:

```python
EDGE = {"X-Edge-Key": "edge-dev-key"}


def test_edge_to_central_sync_end_to_end(client, admin_headers, staff_headers, make_reading, db_session):
    from app.models import Outbox
    from app.services.sync import serialize_session, unsynced_batch

    # một xe vào tạo phiên và outbox
    reading = make_reading(plate="51A90001", direction="in")
    client.post("/sessions/entry", json={"reading_id": reading.id}, headers=staff_headers)
    batch = unsynced_batch(db_session)
    assert len(batch) >= 1

    # đẩy batch lên tầng nhận qua /sync/push, idempotent khi gửi lại
    items = [{"entity_type": o.entity_type, "payload": o.payload} for o in batch]
    r1 = client.post("/sync/push", json={"items": items}, headers=EDGE)
    assert r1.status_code == 200
    client.post("/sync/push", json={"items": items}, headers=EDGE)  # lần hai không nhân đôi

    # tra cứu liên bãi thấy phiên đã đồng bộ, không trùng
    uuids = [o.entity_uuid for o in batch]
    rows = client.get("/central/sessions?plate=51A90001", headers=admin_headers).json()
    matched = [r for r in rows if r["uuid"] in uuids]
    assert len({r["uuid"] for r in matched}) == len(set(uuids))

    # config pull chạy
    cfg = client.get("/sync/config", headers=EDGE).json()
    assert "feature_toggles" in cfg and "price_rules" in cfg
```

- [ ] **Step 2: Run the acceptance test**

Run: `cd src/backend && python -m pytest tests/test_phase6_sync_acceptance.py -v`
Expected: PASS (1 test).

- [ ] **Step 3: Run the full suite**

Run: `cd src/backend && python -m pytest -q`
Expected: PASS toàn bộ (mọi phase).

- [ ] **Step 4: Commit**

```bash
git add src/backend/tests/test_phase6_sync_acceptance.py
git commit -m "test(backend): phase 6 edge to central sync acceptance"
```

---

## Self-Review

**Spec coverage (spec kiến trúc mục 4, 9; frontend v2 mục 5.10):** định danh UUID (Task 1), outbox bền (Task 1, 2, 3), đẩy idempotent (Task 2, 4), config pull (Task 4), tra cứu liên bãi (Task 5). Ngoài phạm vi lõi này (ghi rõ ở đầu plan): đồng bộ đa thực thể (thanh toán, sự cố, ảnh mã hóa), dịch vụ cloud tách riêng và dashboard cloud, tiến trình đồng bộ nền tự động, xác thực khóa thiết bị theo từng bãi. Đây là mở rộng có chủ đích, nên cân nhắc một spec riêng cho tầng cloud.

**Placeholder scan:** không có TBD; mọi bước có code thật. Test `test_push_upserts_idempotently` xác minh không lỗi và không trùng qua tra cứu ở acceptance.

**Type consistency:** `serialize_session`, `upsert_session_from_sync`, `enqueue_session`, `unsynced_batch`, `mark_synced` khớp giữa service (Task 2), wiring (Task 3), endpoint (Task 4), và acceptance (Task 6). Payload dùng chung khóa `uuid, lot_id, zone_id, plate_hash, plate_ciphertext, vehicle_group, vehicle_type, status, fee_amount, match_flag, entry_time, exit_time` giữa serialize (Task 2), test push (Task 4), và test central (Task 5). `plate_hash` chuẩn hóa dùng nhất quán ở tra cứu (Task 5).

**Ghi chú rủi ro:** `session.uuid` là khóa nghiệp vụ song song khóa chính số nguyên; ở tầng nhận (cloud thật) khóa chính số nguyên cục bộ của cloud khác edge, nhưng `uuid` unique bảo đảm idempotent. Khi mở rộng sang nhiều thực thể, mỗi thực thể cần `uuid` riêng theo cùng khuôn mẫu.
