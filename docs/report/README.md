# Report — Báo cáo đồ án

Final thesis format is LaTeX (UIT template, tiếng Việt, IEEE citations, built with XeLaTeX) per `CLAUDE.md`. **For now, during active drafting, chapters are written as Markdown** (`chapters/*.md`) — faster to iterate/review. The LaTeX setup (`main.tex`, `refs.bib`, UIT template) is deferred until formal typesetting is actually needed — it was scaffolded and verified to compile once already, so standing it back up later is low-risk.

- Written stage-by-stage: each project phase ends with its corresponding chapter drafted here.
- Use the `thesis-writer` project skill for all report work.
