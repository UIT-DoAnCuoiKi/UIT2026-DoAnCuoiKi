# Phụ thuộc runtime khi bật INFERENCE_ENGINE=ml (adapter app/services/ml_inference.py).
# Chạy các model đã huấn luyện ở src/ml: detector YOLO (.pt) qua ultralytics,
# OCR CRNN (.onnx) và classifier kiểu dáng (.onnx) qua onnxruntime.
# Cài CHỒNG lên requirements.txt trong cùng môi trường chạy uvicorn:
#   pip install -r requirements.txt -r requirements-ml.txt
# ultralytics kéo theo torch, torchvision, opencv-python, pillow, numpy.
ultralytics==8.4.37
onnxruntime>=1.20
# Adapter tái dùng build_transforms (training/classifier.py) và decode_greedy
# (training/ocr_model.py); hai module này import sklearn/pandas ở mức module.
scikit-learn>=1.3
pandas>=2.0
